# dbs-reset 实施过程

- 请求：在官方 `dbskill` 中新增 `dbs-reset`，实现 Codex Reset Credit 查询与显式确认后的上游重置；使用本机 Codex 登录做只读验证；不执行真实重置；发布到用户 GitHub 账号下的 fork。
- 范围：`skills/dbs-reset`、Marketplace/全量入口、文档计数与入口、构建与校验脚本、发布验证记录。
- 硬约束：不读取或提交凭证值；测试最多调用查询接口和 dry-run；consume 必须显式 `--confirm`；同一次重试复用幂等键；不把代理本地 cooldown 清理冒充上游 reset。
- 初始状态：本地 `main` 已快进到官方 `dontbesilent2025/dbskill` 的 v2.18.40；工作树干净。原定同名 GitHub 仓库已存在且不是 fork，因此创建 `zenenznze/dbskill-codex-reset` fork。
- 已完成决策：优先复用官方接口契约；参考外部只读 Skill 的脱敏与过期时间处理，但重新实现；`available_count` 不用详情行数替代；将读与写路径分离。
- 当前检查点：代码、文档、入口、v2.18.41 发布字段和报告已写入工作树；静态、触发、单元、构建、本机只读查询、dry-run 和保护门均已通过；下一步是精确 commit、push 到 `origin/main` 并回读远端。
- 交接：见 `交接文档/dbs-reset-20260909.md`。
